namespace Grained.Domain.Enums;

// Standard = a teacher-awarded badge for effort/character on the day.
// Achievement = a bigger milestone a ChurchAdmin awards (baptism, memorised a book, finished a unit).
public enum BadgeTier
{
    Standard = 0,
    Achievement = 1
}
